# 西ローマ帝国末期シミュレーション

## このプロジェクトについて

395年（テオドシウス1世の死による帝国分割）から476年（ロムルス・アウグストゥルス廃位）までの
西ローマ帝国を、実権者の立場で運営するターン制シミュレーション。

- **1ターン = 1年**、全81ターン
- **1プレイの想定時間 = 2〜3時間**（1ターンあたり約2分）
- **プレイヤー** = 西ローマ帝国の実権者（皇帝およびその宮廷）
- **中心となる遊び** = 外交と戦争。内政は外交・軍事の原資を作るための手段として扱う

## 描きたい主題

このゲームが表現したいのは、次の2つのジレンマ。**すべての数値設計はこの2点に奉仕する。**

1. **循環の罠** — 軍を維持するには金が要る。金を生むのは属州。属州を守るには軍が要る。
   属州をひとつ失うと軍が縮み、軍が縮むと次の属州を失う。この下降スパイラルが体感できること。
2. **短期と長期の取引** — 蛮族を傭兵（フォエデラティ）として雇えば、目先の戦線は安く埋まる。
   だが給金が途切れれば寝返り、定住を許せば税基盤と正統性が削れていく。
   「今日を凌ぐ判断が、10年後の帝国を殺す」という構造をプレイヤーに実感させる。

勝利は「拡大」ではなく「延命」。史実より長く保たせることが目標になる。

## 状態モデル

パラメータは7つに固定する。**増やさない。**

| パラメータ | 意味 | 主な増減要因 |
|---|---|---|
| `treasury` | 国庫（ソリドゥス） | 税収 − 軍維持費 − 貢納 − 宮廷費 |
| `taxBase` | 税基盤 | 属州の支配度に比例。略奪・蛮族定住で恒久的に減る |
| `fieldArmy` | 野戦軍（コミタテンセス） | 徴募・戦闘損耗。維持費が最大の支出 |
| `legitimacy` | 正統性 | 勝利・元老院への譲歩で上昇。敗戦・簒奪未遂・属州喪失で低下 |
| `senateSupport` | 元老院貴族の支持 | 増税で低下、免税特権の追認で上昇 |
| `eastRelations` | 東ローマとの関係 | 援軍要請・帝位承認・利害衝突で変動 |
| `foederatiLoyalty` | フォエデラティの忠誠 | 給金の支払い実績に連動。未払いが続くと反乱 |

君主と王朝（`ruler` / `dynasty`）は7パラメータには含めず、`GameState` 内の別サブ構造として持つ。

### 属州

`Gallia` / `Hispania` / `Britannia` / `Africa` / `Italia` / `Illyricum` / `Noricum`

各属州は `control`（0〜100）、`baseTax`、`garrison` を持つ。
`Africa` は穀物供給源で、失うと `Italia` の `control` に恒久ペナルティ（史実の429年以降を再現）。

### 蛮族勢力

**14勢力。定住地を持つ民と、動き続ける民に分ける。**

| 郷里を持つ（地図では領域） | 郷里を持たない（地図では駒） |
|---|---|
| フランク・ブルグント・スエビ・サクソン・ゲピード・スコティ | 西ゴート・ヴァンダル・フン・アラン・東ゴート・ヘルール・アラマンニ・マウリ |

マウリだけは**略奪だけを行う民**として持つ（`raider`）。定住せず、
支配度を奪い切らず、掠めたその年のうちに境外へ引き揚げ、境外での
成長にも上限がある。属州を奪って王国を建てる型に当てはめたときは
アフリカに常設の戦線ができ、穀物供給の途絶で生存率が中級 17% → 2% に
落ちた。上限は必ず `MIN_STRENGTH_TO_ADVANCE` より大きく取ること
（45 にしたときは閾値に届かず一度も山を降りない飾りになった）。

フンとアランは遊牧民、西ゴートとヴァンダルはこの時代ずっと移動を
続けていた民で、帝国外に攻め込める定住地を持たない。アラマンニの地
（アグリ・デクマテス）とマウリの地（マウレタニア内陸）は国境線や
属州そのものと重なっていて、国単位の地図では分けられないため同じく面を持たない。

駒の位置は395年前後の所在に合わせる。ゴート族はドナウ下流から
黒海北岸、東ゴート（グロイトゥンギ）はドニエストル〜ドン川のあいだ、
フン本体はさらに東のポントス・カスピ海草原、アランは北カフカスに置く。

各勢力は `strength`、`stance`（`hostile` / `foederati` / `settled`）、`demand`（金・土地・称号）を持つ。
`settled` になった勢力は敵ではなくなるが、その土地の `baseTax` は永久に失われる。

族長は**在位ごとの軍事能力**を持つ。君主能力と同じ考え方で新しい資源ではなく、
戦闘解決の攻撃側戦力に掛かる補正としてのみ作用させる。史実の事績に即して
アラリック9・ガイセリック10・アッティラ10、ウルディン5のように置き、
`data/leaders.json` から年で引く。この模型には年ごとの戦力の推移が無いので、
フンの脅威が440年代に頂点を迎えることは開始戦力ではなく族長の能力で表す。

### 郷里（本拠地）

各勢力は帝国外に郷里を持ち、`control` / `baseTax` / `garrison` / `owner` を持つ。
属州と同じく7パラメータには足さず `GameState` の別サブ構造（`homelands`）に置く。
`baseTax` は属州（40〜220）より低い。ローマの版図の外で徴税機構が育っておらず、
奪っても属州ほどの実入りにはならない。

**遠征は属州の防衛よりはるかに重い。** 守るのは「その土地の守備隊 ＋ その勢力の
戦力の半分 ＋ 他の敵対勢力の加勢」で、加勢は奪った郷里の数だけ固くなる
（`COALITION_DEFENSE_SHARE` × `COALITION_RALLY_PER_HOMELAND`）。
連合が固くなる項が無かったときは、一度連合を破った帝国がそのまま境外を平らげ、
初級で8勢力すべての郷里を取る局が出た。契約中のフォエデラティの郷里は
攻められない（味方をひととおり作ってから境外を平らげる抜け道になるため）。

計測（300試行、`conqueror` 方針）。郷里をひとつでも取った局の割合:

| | 初級 | 中級 | 上級 |
|---|---|---|---|
| 郷里を取った局 | 29% | 4% | 0% |

生存率は `general` の 79/38/15 に対し `conqueror` は 74/23/9 で、
**どの難易度でも遠征は生存を削る。** 統一シナリオ（12/4/0%）と同じ水準に
収まっており、拡大がこの帝国に割に合わない点は変わっていない。

#### 併合した郷里は総督を置いて治める

奪った土地を放っておくと元の主が取り返しに来る。属州と同じく総督の席を
置けるようにして、**新しい仕組みは足さない。** 席は征服したその年にでき、
取り返された年に消える。

| 作用先 | 中身 |
|---|---|
| 奪還の確率 | `governorDefenseModifier` で割る |
| 支配度の自然回復 | `governorControlRecoveryModifier` を掛ける |

属州では総督の能力を守備隊の**戦闘力**に掛けているが、郷里の奪還は
戦闘解決を経ない一発の判定なので掛ける先が無い。同じ補正を確率のほうに
効かせている。

**郷里の席からは僭称帝国が立たない。** 総督の反乱の判定は `state.governors`
ではなく `state.provinces` を回す。ローマの属州ですらない境外の土地から
「ガリア帝国」の類が生まれるのは筋が通らないし、僭称帝国は属州の収入と
保持属州数を前提にした仕組みなので、郷里を混ぜると数が合わなくなる。

計測（300試行、初級、`conqueror` 方針。郷里を保った年で見る）:

| | 総督を置く | 置かない |
|---|---|---|
| 平均支配度 | **58.5** | 39.9 |
| 支配度が削られた年 | 12 | 19 |

## シナリオ

**史実（`historical`）／統一（`reunification`）** から選ぶ。

- **史実** — 上に書いた本編そのもの。勝利は延命で、拡大の手段はない。
  東ローマは属州も軍も持たず `eastRelations` という数値としてだけ働く
- **統一** — 「東を併合できた世界線」。東ローマに宣戦して東方属州を奪い、
  ローマを再統一する。**統一は局を終わらせない**（`unifiedYear` に
  記録するだけの節目）

**終わりの年はシナリオごとに違う**（`SCENARIO_ENDING_YEAR`）。
史実は476年（全81ターン）、統一は**565年（全170ターン）**。
565年はユスティニアヌス1世の没年で、統一の世界線は
6世紀の東方からの巻き返しを迎え撃つところまでを1局とする。

**統一シナリオは史実側の数値に一切影響させない。** 調整済みのバランスは
本編のものなので、同一シードで1行も変わらないことを変更のたびに確認する
（`npm run sim -- --seed 12345 --out` の CSV を突き合わせる）。

東ローマとペルシアは7パラメータには含めず、`dynasty` / `general` と同じく
`GameState` の別サブ構造として持つ。東方属州も西の `provinces` には混ぜず
`east.provinces` に置き、`owner` が `west` になったものだけを収入と
保持属州数に数える。

### 東方属州

`Thracia` / `Asiana` / `Oriens` / `Aegyptus`。`owner` は `east` / `west` / `persia`。

### ペルシアとの修好

`persia.relations`（0〜100、初期20）を `persia` サブ構造に持つ。
7パラメータには足さない。**新しい資源にもしない。** 上がった関係は
既存の2つの確率——介入を始める判定と、動き出したあとの毎年の攻勢——に
掛かる補正としてのみ働く。

**どちらもゼロにはできない**（`PERSIA_RELATIONS_INTERVENTION_FLOOR` 0.45 /
`PERSIA_RELATIONS_ATTACK_FLOOR` 0.55）。止められてしまうとラスボスを
金で消せることになり、下の主題が壊れる。介入したあとの修好は効きが半減する。

史実の西ローマとサーサーン朝にはほとんど直接の往来が無かったので、
東ローマとの関係（初期60）より冷たいところから始める。

**行動枠を消費する。** 計測では、徴募より先に使節へ枠を割く方針AIにすると
統一率が 4% → 0% に落ちた。2つしかない枠を毎年の使節に食われ、
東の野戦軍（175）を上回る兵力を作れなくなるため。
使節と軍備が枠を争うこと自体は設計どおりの取引。

### 6世紀 — ユスティニアヌスとベリサリウス

統一シナリオの山場は5世紀の征服ではなく、**そのあと89年保たせられるか**。

**527年、ユスティニアヌス1世が西方の回復（renovatio imperii）を掲げて動く。**
東方属州を西に握られていれば、**講和していようと従属していようと**
東は戦端を開く（`JUSTINIAN_RECONQUEST_YEAR`）。これが無かったときは、
統一したあと講和してしまえば東は二度と動かず、6世紀が何も起きない89年だった。

将は史実の顔ぶれを `data/leaders.json` から年で引く。
**ベリサリウス（軍事10）** が527〜559年、**ナルセス（軍事9）** が559年以降。
5世紀の東の司令官が5〜7なので、6世紀だけ格が違う。

司令官の力量は**戦力だけでなく攻め返す頻度にも**効かせる
（`EAST_COMMANDER_TEMPO_PER_POINT`）。戦力補正だけだと
ベリサリウスとイッルスの差が 1.20 対 1.04 にしかならず、
6世紀が別の時代に見えなかった。有能な将は一撃が重いだけでなく、
休まず戦役を起こす。

**東の軍とペルシアの戦力には天井を置く**（`EAST_ARMY_MAX` /
`PERSIA_MAX_STRENGTH`）。掛け算だけで伸ばしていたので、170ターンでは
東の軍が28倍まで膨らんだ。同時に**最低限の立て直し**（`EAST_ARMY_REBUILD`）も
足してある。掛け算だけだと一度壊滅させた東は二度と戻れず、
ユスティニアヌスの巻き返しそのものが起こらないため。

計測（400試行、初級、`unifier` 方針）:

| | |
|---|---|
| 527年に到達した局 | 28（西が握る東方属州 平均 1.39） |
| 再征服が発火した局 | 20 |
| 565年に到達した局 | 21（同 平均 **0.67**） |

**東は西が握った東方属州のおよそ半分を6世紀に取り返す。**
5世紀を生き延びた局のうち、565年まで残るのは約半分になる
（初級 476年到達12% → 565年到達6%、中級 4% → 2%）。

### ペルシアはラスボスとして機能させる

**ペルシアが握る属州は、東ローマと講和したあとでも攻められる。**
侵攻に一律「東ローマと交戦中」を要求していたときは、ペルシアが1州でも
持っている状態で講和すると取り返す手段が消え、統一が永久に成立しなく
なっていた。ペルシアの側は介入済みなら講和に関わらず攻勢を続けるので、
一方的に殴られ続ける形にもなる。「講和してもペルシアは引かない」という
行動の説明とも食い違っていた。判定は `canInvadeEastProvince` に集約し、
UI の候補もそこから引く。

**介入した年に必ず橋頭堡を1州確保させる。** 確率的に削るだけにしていたときは、
西が東を平らげるほうが速く、統一した58局のうちペルシアと属州を争ったのは
2局だけだった。それでは間に合えば無視できる背景であってラスボスではない。
現在は統一した50局のうち49局がペルシアから属州を取り返している。

計測（`unifier` 方針、300試行）:

| 難易度 | 統一 | 統一年 |
|---|---|---|
| 初級 | 17% | 409–453 |
| 中級 | 4% | 410–446 |
| 上級 | 1% | 414–415 |

本編（85/47/19%）よりはるかに難しい。拡大はこの帝国には本来無理だった、
という本編の主題をそのまま残す水準にしてある。

## 難易度

**初級 / 中級 / 上級**（`beginner` / `standard` / `veteran`）から選ぶ。

難易度は**新しいメカニクスを足さない。** 既存の計算式に掛かる倍率としてのみ作用させる。
君主能力の補正と同じ考え方で、**中級はすべて 1.0**。
調整済みの基準バランスがそのまま中級になる。

触る対象は「主題」の2つのジレンマに直結する3点と、史実展開の再現度の
合計**4点に絞る。増やさない。**

| 倍率 | 作用する既存の計算式 | 対応する主題 |
|---|---|---|
| `incomeMultiplier` | 税収 | 循環の罠 |
| `barbarianPowerMultiplier` | 戦闘解決（攻撃側戦力） | 循環の罠 |
| `foederatiEscalationMultiplier` | フォエデラティの給金要求の膨張率 | 短期と長期の取引 |
| `historicalSeverityMultiplier` | 有害な歴史イベントの発火確率と被害量 | 史実展開の再現度 |

### 史実展開の再現度

`historicalSeverityMultiplier` は **上級 1.0 / 中級 0.6 / 初級 0.3**。

- **上級** — 史実に近い。406年のライン渡河も429年のアフリカ渡海もほぼ確実に起き、
  帝国は史実通り崩壊に向かう
- **中級** — 史実より西ローマ有利。災厄は半分程度しか起きず、起きても被害は6割
- **初級** — 史実よりかなり西ローマ有利。災厄はめったに起きない

緩和は `harmful: true` のイベントにだけ掛ける。451年カタラウヌムの戦いのような
**有益なイベントまで弱めると初級のほうが不利になる**ため。
条件を満たしても見送ったイベントは `firedEventIds` に入れないので、
条件が続く限り翌年以降に改めて判定される。

難易度は7パラメータには含めず、`GameState` のプレイ開始時の設定として持つ。
**スコア結果に難易度を必ず記録する。** 異なる難易度のスコアは比較できない。

## 王朝システム

### 君主の能力

能力は「軍事」「統治」「交渉」の**3つのみ。増やさない。** 各1〜10。

能力は**新しい資源ではない。** 既存の計算式に対する補正倍率としてのみ作用させる。

| 能力 | 作用する既存の計算式 |
|---|---|
| 軍事 | 戦闘解決（防御側戦力の補正） |
| 統治 | 税収、`legitimacy` の自然減 |
| 交渉 | 貢納コスト、交渉の成功率 |

### 名前

君主は名前を持ち、**プレイヤーが付けられる。**

- 開始時に初代の名を入力する（既定はデータ側の初期君主）
- 在位中はいつでも改名できる。表示だけの変更でどの計算式にも影響しない
- 代替わりした皇帝の名は `dynasty.namePool` から自動で引く。
  引いた名は候補から取り除く。引きっぱなしにすると1回のプレイで
  同じ名の皇帝が何人も出て年代記が読めなくなる
- 名前の候補は `data/dynasty.json` に置く。コードに直接書かない

### 寿命と継承

- 寿命は `seed` 経由の乱数で決める。`tick()` の純粋性を壊さない。
- 能力もランダムだが、上下限は `constants.ts` に置き、極端な値が出ないようにする。
  ただし**10%で能力7〜10の名君**が出る（`EXCEPTIONAL_RULER_PROBABILITY`）。
  通常の抽選は3〜8なので、これが無いと9・10の君主が決して生まれず、
  ユスティニアヌスを擁する東に対して一方的に不利だった。
- 在位が極端に短い連続交代を避けるため、**最低在位年数**を保証する。
- **暗殺判定**を持つ。発生確率は `legitimacy` が低いほど上がる。
  この時代の皇帝の死因として自然死より妥当で、既存の `legitimacy` を機能させられる。
- 継承時の処理:
  - 成人した嫡子がいれば継承。`legitimacy` の低下は小さい。
  - いなければ**継承危機**とし、`legitimacy` を大きく下げ、簒奪者イベントの確率を上げる。

### 婚姻外交

「婚姻同盟」アクションは抽象的な効果ではなく、実際の人物同士の婚姻として扱う。

- 相手は蛮族勢力の族長家、または東ローマ帝室。
- **蛮族との婚姻** — 相手の `stance` と `foederatiLoyalty` が改善するが `senateSupport` が低下する。
- **東ローマとの婚姻** — `eastRelations` と `legitimacy` が上昇するが、成立の難易度が高い。
- 効果は即時のものと、**子が生まれてから発生する**ものを分ける。
- 混血の後継者は `legitimacy` に負の補正がつく一方、その勢力に対する**請求権**を得る。
- 婚姻は**行動枠を消費する**（根拠は下記「行動枠について」）。

### 能力の変更口

設定から君主能力を変更できる関数を `core/` に持つ。UIは作らない。
変更を適用したセーブには**フラグを立て**、スコア結果に「調整済み」と記録する。
記録がないと以降のスコア比較が意味を失う。

## 軍司令官（マギステル・ミリトゥム）

395〜476年の西ローマを実際に動かしていたのは皇帝ではなくこの職で、
スティリコもアエティウスもリキメルも皇帝ではない。

君主と同じく7パラメータには含めず、`GameState` の別サブ構造として持つ。
**新しい資源ではない。** 既存の計算式に対する補正としてのみ作用させる。

| 作用先 | 効果 |
|---|---|
| 戦闘解決の防御側戦力 | 有能なら強く、空位なら弱い |
| `legitimacy` の自然減 | 有能なほど余分に減る |
| 撃退で得る `legitimacy` | 有能なほど将軍の手柄になり皇帝に入らない |
| 簒奪者の確率 | 有能なほど上がる。在職中なら簒奪者は将軍本人 |

### 西にも名将を輩出させる

東は6世紀にベリサリウス（軍事10）とナルセス（軍事9）を**確実に**得る。
西の側にも桁違いの人物が出る目を用意しないと、一方的に不利になる。
足すのは新しい仕組みではなく、**抽選の幅と史実の名簿だけ。**

**史実の軍司令官はその在職の年に迎えられる**（`data/leaders.json` の
`westCommanders`）。東の `eastCommanders` と同じ作りで、
一度仕えた将は二度は出ない。

| 将 | 在職 | 軍事 |
|---|---|---|
| スティリコ | 395–408 | 9 |
| コンスタンティウス3世 | 411–421 | 8 |
| フラウィウス・アエティウス | 425–454 | **10** |
| リキメル | 457–472 | 8 |
| オレステス | 472–476 | 6 |

残り任期が5年を切る年に任命したときは通常の抽選に落とす
（アエティウスを453年に迎えて1年で退かれても意味がないため）。

史実の将がいない年——とくに統一シナリオの6世紀——は通常の抽選だが、
**12%で軍事10の将が出る**（`EXCEPTIONAL_GENERAL_PROBABILITY`）。
君主も**10%で能力7〜10の名君**になる（`EXCEPTIONAL_RULER_PROBABILITY`）。
マヨリアヌスのような「稀に出た有能な帝」を、範囲を広げることで表す。

計測（300試行、中級、`general` 方針）:

| | |
|---|---|
| 史実の将を1人以上迎えた局 | 61% |
| 軍事10の将が出た割合 | 15%（延べ1196人） |
| 能力9以上の君主が出た割合 | 6%（延べ1154人） |
| 1局で得た最良の将（平均） | 軍事 9.06 |

**名将を得ても生存率はほとんど動かない**（51/23/14 → 53/22/11、
n=300 の誤差の範囲）。有能な将ほど正統性を余分に削り、戦勝の名声を
持っていき、簒奪の確率を上げるので、**強い将は純粋な上振れではない**。
効いたのは統一シナリオの6世紀のほうで、中級の生存率が 0% → 2%、
統一率が 3% → 5% に上がった。

- 任期を終えると自ら退く。**後任は自動では決まらない。**
  空位のままだと防衛が弱くなるので、任命するかがプレイヤーの判断になる
- 解任すると `legitimacy` は戻るが、その将に従っていた兵は離れる

**戦勝の名声を将軍が持っていく形にすることが要になる。** これが無いと
「強い将軍を置く → 勝つ → 正統性が回復する」で自己強化してしまい、
名将を抱えても最終正統性が空位のときと変わらなかった（どちらも83）。

計測（300試行、将軍の扱い方だけを変えて比較）:

| 方針 | 中級 | 上級 |
|---|---|---|
| 正統性が危ういときだけ切る | 47% | 19% |
| 任命して抱え続ける | 45% | 19% |
| 空位のまま放置 | 40% | 15% |
| 名将を無条件に切る | 27% | 6% |

切る時機を選ぶのが最良、無条件に切るのが最悪という順になっている。
史実の408年（スティリコ処刑）・454年（アエティウス謀殺）が
いずれも崩壊を早めたことと符合する。

451年カタラウヌムの発火に軍司令官の在職を要求しているので、
空位のまま放置する方針はこの勝利も取り逃す。

## 官職（プラエトリア長官・属州総督）

**プラエトリア長官は軍の指揮官ではない。** 近衛隊は312年に解散しており、
395〜476年の praefectus praetorio は税務・軍糧・属州行政を統べる文官の筆頭。
軍を率いるのはマギステル・ミリトゥムのほうで、役割は重ならない。

君主・軍司令官と同じく7パラメータには含めず、`GameState` の別サブ構造として持つ。
**新しい資源ではない。** 既存の計算式への補正としてのみ作用させる。

| 官職 | 作用する既存の計算式 |
|---|---|
| 長官 | 税収、元老院支持の自然減 |
| 総督 | その属州の守備隊の戦闘力、支配度の自然回復 |

**能力と野心を別の軸にする。** 任命は候補3人から選ぶので、
「能力7・野心8 を採るか、能力5・野心5 にしておくか」が判断になる。
野心は反乱の確率にのみ効き、職務には一切効かない。

### 任命は行動枠を消費しない

長官1人と総督7人（併合した郷里があればその数だけ増える）は任期が
ばらばらに切れるため、枠を食わせると
ほぼ毎年どちらかの任命に追われ、派遣も徴募もできなくなる。
計測では枠を消費させた場合に生存率が中級 37% → 24% とかえって悪化した。
任命は勅令一本の話であって1年を費やす行動ではない。

無償ではない。任命には金がかかり、野心の高い者を選べば反乱が増える。
**解任のほうは枠を消費させる**（正統性が戻る政治的な行為なので無料の上振れにしない）。

### 空位から始めない

この時代の帝国に長官も総督もいなかったことはないので、
空位は退任や反乱の**結果**としてのみ起きるものとして扱う。
空位を既定にすると調整済みバランスに恒久的な減収と守備の罰が
掛かりっぱなしになり、生存率が中級 47% → 28% まで落ちた。
初期の顔ぶれは `data/officials.json` に置く。

### 反乱

**属州総督の反乱**と**皇帝の兄弟（傍系の一族）の挙兵**の2種類。
**正統性に関わらず毎年判定する。** 正統性の低さは確率を押し上げる要因として
効かせ、押し上げは線形（閾値を上回れば0、正統性0で最大）。
西ローマの崩壊が中央からではなく属州の僭称帝から始まったこと
（ガリアのコンスタンティヌス3世、ブリタンニアの相次ぐ僭称）を表す。

当初は正統性が閾値を下回った年にだけ判定していたが、それだと
順調な帝国では一度も起きず、「属州はいつ離れてもおかしくない」という
緊張が出なかった（総督の反乱が起きた局は27%）。毎年判定に変えて
基礎確率を大きく下げてある（総督 0.01 → 0.0007）。

- **総督の反乱** — その属州の支配度と守備隊の半分を失う。守りが薄くなるので、
  蛮族に狙われている属州で起きるとそのまま失陥につながる
- **兄弟の挙兵** — 野戦軍の一部が離れ、本人は継承候補から外れる。
  後継者を抱えることは継承危機を防ぐ利点だが、同時に帝位を狙う者を
  抱えることでもある、という取引をここで成立させる

計測（300試行、`general` 方針）。1局あたり総督の反乱が約2件:

| | 起きた局の割合 |
|---|---|
| 総督の反乱 | 71% |
| 兄弟の挙兵 | 37% |

## コアループ

`tick(state, actions, seed) => newState` の中で、必ずこの順序で処理する。

1. 収入 — 各属州 `control × baseTax × 徴税率`
2. 支出 — 軍維持費、貢納、宮廷費。国庫が負なら軍の脱走判定
3. プレイヤー行動の適用
4. 蛮族AIの行動 — 評価関数で移動・略奪・定住・侵攻・寝返りを決定
5. 戦闘解決
6. 支配度と税基盤の更新
7. 正統性判定 — 閾値を下回ると簒奪者イベント
8. 王朝の更新 — 加齢、寿命・暗殺判定、継承処理、軍司令官の任期
9. 歴史イベントテーブルの発火判定

## プレイヤーのアクション

1ターンに選べるのは**2つまで**。何を諦めるかを選ばせることがこのゲームの本体。

- **交渉** — 貢納で和平を買う／土地を与えて定住させる／婚姻同盟／突きつけられた要求に応じる
- **雇用** — フォエデラティ契約（安いが `foederatiLoyalty` に依存）
- **軍事** — 野戦軍の派遣、属州防衛、徴募、**属州での募兵**、軍司令官の任命・解任、
  蛮族の郷里への遠征、**会戦**、**僭称帝国の討伐**
- **内政** — 徴税強化、軍の再編、**元老院の機嫌を取る3手**
- **東帝国** — 援軍要請、帝位の承認取り付け、修好（関係の回復）。
  統一シナリオではさらに宣戦・東方への侵攻・講和、
  そして**サーサーン朝への修好**

### 属州で募兵する

従来の徴募は国庫から一律の兵を買う手だったので、どの属州を持っていても
同じだけ兵が増えた。**属州で募兵する**手はその土地から兵を取る。

- 費用は一律（`PROVINCE_RECRUIT_COST`）だが、得られる兵は
  `baseTax × 支配度 × PROVINCE_RECRUIT_PER_BASE_TAX`
- 兵は野戦軍とその属州の守備隊に分けて入る（`PROVINCE_RECRUIT_GARRISON_SHARE`）
- その属州の支配度が下がる（`PROVINCE_RECRUIT_CONTROL_LOSS`）

**豊かで落ち着いた属州ほど兵が出る。** 同じ70ソリドゥスで
ガリア（税収基礎180・支配90）は野戦軍 +9.1、ブリタンニア（40・60）は
+1.3 にしかならない。属州を失うと軍が縮むという「循環の罠」を、
徴募の側からも触れる形にしてある。属州を失えば兵の出どころそのものが減る。

### 元老院の機嫌を取る

手は3つあり、**差し出すものがそれぞれ違う。**「何を差し出して機嫌を取るか」を
選ばせることが狙いなので、どれか1つが常に得になってはいけない。

| 行動 | 差し出すもの | 元老院 | 正統性 |
|---|---|---|---|
| 元老院に譲歩（免税特権の追認） | `taxBase` −2（恒久） | +12 | +4 |
| 競技会を催す（ludi） | `treasury` −90 | +9 | +6 |
| 執政官位を授ける | `legitimacy` −5 | **+16** | −5 |

執政官はこの時代には実権のない名誉職で、皇帝が毎年貴族に与える褒賞だった。
金も土地も要らない代わりに、**その年の栄誉は皇帝ではなくその貴族のものになる。**
戦勝の名声を軍司令官が持っていくのと同じ構図を、元老院に対して作っている。

競技会は民衆も沸くので正統性への効きが大きい。国庫を削るので、
軍を養えない年には選べない。

### 蛮族の要求

属州に居座る敵対勢力は、金・土地・称号のいずれかを要求する。
要求が出るのは蛮族AIの手番なので、答えられるのは翌年になる。

| 要求 | 飲んだときに払うもの | 得るもの |
|---|---|---|
| 金 | `treasury`（相手の戦力に比例） | 属州から引き揚げ、その軍の一部が散る |
| 土地 | その属州の `baseTax` と帝国の `taxBase` | 戦線が消える |
| 称号 | `senateSupport` と `legitimacy` | 味方（フォエデラティ）になる。給金は雇うより安い |

拒否の代償は、その年の戦闘が重くなることと、相手が返事を待たずに
その土地へ住み着きやすくなること。**一時的な罰だけでは足りない。**
当初は放置年数に応じた戦力の複利成長にしていたが、恒久的に資源を削る
応諾のほうが常に損になり、拒否一択のただの難易度税になった
（生存率 52% → 29%）。

**要求への応答は行動枠を消費しない。** 枠を食わせると毎年
`military_deploy` と競合して常に負け、「金・土地・正統性のどれを
差し出すか」ではなく「軍を動かすか答えるか」を選ばせることになる。
無償ではない（応答は必ず何かを恒久的に削る）ので、Task B が警告する
「純粋な上振れ」にはあたらない。

金を飲んだ相手の戦力を減らすのが要だった。引き揚げさせるだけでは
境外で毎年成長して数年で戻り、払う意味がほとんど無かった。

計測（300試行）。金を飲む方針が最良になる:

| 飲むもの | 中級 | 上級 |
|---|---|---|
| 金のみ | 40% | 15% |
| 何も飲まない | 40% | 8% |
| 称号のみ | 31% | 8% |
| 土地のみ | 27% | 7% |
| すべて飲む | 19% | 4% |

差がはっきり出るのは上級で、中級では金と拒否が並ぶ（n=300 の誤差の範囲）。
**土地と称号をまとめて飲む方針が最下位になることが要**で、
「飲めば楽になる」わけではなく、何を飲むかを選ばせる形になっている。

称号が長期で不利なのは意図どおり。フォエデラティを抱えることの
代償（給金の膨張と `taxBase` の恒久的な減少）がそのまま出ている。

### 行動枠について（Task B の診断結果）

行動枠の数を1〜4に振って計測した結果、**枠を増やすと生存率が下がる**
（1枠79% / 2枠63% / 3枠32% / 4枠29%）。枠は不足しておらず、現状は
「何を諦めるか」を選ばせるのではなく自傷を抑える蓋として機能している。
設計意図はまだ成立していない。

この診断を受けて行動価格を調整済み（調整前→後、2枠目の限界価値）:

| 行動 | 調整 | 生存率 | スコア |
|---|---|---|---|
| `military_defend` | 守備+6→+10 | 60%→56%（基準65%→32%） | 負→正に反転 |
| `domestic_reorganize_army` | 兵の純増→守備隊からの再配分 | 100%→67% | 40426→31341 |
| `domestic_appease_senate` | 元老院支持に自然減を追加 | 発火0回→100回 | 死択→生存 |
| `east_confirm_title` | 同上（正統性の自然減） | 発火0回→32回 | 死択→生存 |

`domestic_reorganize_army` は兵を生まず属州 `garrison` から
`fieldArmy` へ移す再配分にした。単価の調整では発火頻度の差
（再編4838回 対 徴募659回）で覆せず、支配性が解消しなかったため。
現在は「兵力は保てるが属州が痩せてスコアは落ちる」取引になっている。

**分散の指標について。** スコアの変動係数（全体）は運の大きさを測る
指標として使えない。勝てば高スコア・負ければほぼ0という二値分布では、
生存率50%付近で必ず1.0前後になる算術的帰結だからである
（理論値 √(p(1-p))/p）。実際の再現性は**生存者のみの変動係数**で見る。
正攻法は全体1.03に対し生存者のみ0.22で、同じ方針なら同程度のスコアに
収束しており運ゲーではない。

婚姻が行動枠を消費するのはこの診断による。無償にすると純粋な上振れだけの
選択肢になり、すでに再編1択に傾いている行動経済をさらに壊すため。

## 会戦と僭称帝国

属州の攻防が「支配度を削り合う」処理なのに対し、**会戦**は野戦軍どうしが
正面からぶつかる一度きりの決戦。相手は帝国領に踏み込んだ蛮族の軍勢、
東ローマの野戦軍、ペルシアの軍のいずれか。

**率いる者が要る。** 皇帝自身（軍事6以上）か軍司令官がいなければ挑めない。
軍を率いたのは皇帝ではなくマギステル・ミリトゥムだった、というこの時代の
形をそのまま条件にしてある。

| 率いる者 | 得るもの | 負うもの |
|---|---|---|
| 皇帝が親征 | 君主の軍事能力がそのまま乗る | 大敗すると35%で**捕虜**になる |
| 軍司令官 | 捕縛の危険がない | 将軍の能力に依存する |

野戦軍の85%を投じるので、勝てば相手の主力を大きく削れるが、負ければ
帝国の主力が一度に失われる。差が大きい負けは**大敗**になり、属州が動揺する。

### 属州から兵を動員する

会戦には**属州の守備隊を呼び寄せられる**（`MOBILIZE_MAX_PROVINCES` まで）。
守備隊の半分が動き（`MOBILIZE_GARRISON_SHARE`）、野戦軍ではないので
戦場での働きは劣る（`MOBILIZE_EFFICIENCY`）。

**新しい兵は生まれない。** `domestic_reorganize_army` と同じく守備隊から
野戦軍への再配分で、動員した属州はその年のあいだ守りが薄くなる。会戦は
たいてい侵入してきた敵と戦う年に選ぶので、**手薄にした属州がそのまま
狙われる**という取引になる。

計測（初級、開始局面、300回）:

| | 勝率 |
|---|---|
| イタリア・ガリアを動員（戦力 +14.0） | **49%** |
| 動員しない（野戦軍の投入分 102） | 28% |

動員は `giveBattle()` と `openBattlefield()` の両方が同じ
`mobilizedStrength()` を引くので、戦闘画面を開いても開かなくても
足される兵は変わらない。

### 戦闘画面（戦列マップ）

会戦を選んだ年は**戦闘専用の画面に切り替わる。** 左翼・中央・右翼の
3つの戦列に歩兵・騎兵・弓を配り、激突ごとに前進・迂回・退却を選ぶ。

**戦場は地図で見せる。** 関ヶ原の布陣図と同じ読み方にする。
地形の上に両軍の隊を置き、**隊は兵の列そのもの**として描く。
列の**幅が兵力**、色が陣営、札の下の帯が士気を表す。命令は隊から伸びる
**矢**で描くので、交える前に「どの戦列がどこへ向かうか」が図の上で分かる。

- **布陣** — 控えの隊を選び、図の戦列に触れて置く。置いた隊に触れると控えに戻る
- **命令** — 図の戦列に触れて選ぶ。選んだ命令はその場で矢に反映される
- 矢の向かう先は `resolveTarget()` を core から引く。**表示側で引き写さない**
  （正面が空いた戦列が隣へ回り込む規則を描き落とすため）。
  迂回は破線、正面が空いて隣へ流れたときは実線にして見分けられるようにする

**兵は兵科ごとの姿で描く。** 一目でどの兵科か分かることを優先する。

- **歩兵** — 楯（スクトゥム）を並べた密集陣。**3列**に組み、兜と槍の穂先を立てる
- **騎兵** — 馬を横向きに。胴・首・頭・四肢と尾を取り、鞍上に槍を構えた騎手を乗せる。
  後列を半歩ずらし、馬体が重なっても数が読めるようにする
- **弓兵** — **2列**の散兵線。弓を C 字に、弦と矢を添えて射る姿にする

数万人の兵を1人ずつは描けないので、**列の幅で兵力**を表す。
1人あたりを小さく取って**数を多く**並べるほうが軍勢に見えるので、
兵の大きさより頭数を優先する。列を増やしたぶん兵数の札は下げてある。

味方は**ローマ軍**と呼ぶ。「我が軍」では、東ローマや僭称帝国と
戦うときにどちらもローマ軍になって区別が付かない。

**本陣にはラバルムを立てる。** 指揮官の控える位置を図の下端に取り、
竿頭のキリストのモノグラム（☧）と帝室紫の布、天幕、率いる者の名を並べる。
地図・軍団の竿頭・君主の欄と同じ図（`ChiRho`）を使い回す。
当初は中央の戦列の真下に立てたが、「中央」の札と竿が重なったので
**戦列の名より下に専用の帯**を取ってある。

**兵数は千人・万人で出す**（`MEN_PER_STRENGTH` = 1000）。西ローマの
野戦軍はノティティア・ディグニタトゥムの記載でおよそ10万規模なので、
開始時の野戦軍 120〜150 がその桁に収まる。**表示のためだけの倍率で、
どの計算式にも掛からない。**

**敵は上端から下へ、我が軍は下端から上へ積む。** どちらも上端から
積んでいたときは、隊が1つずつしかない普通の布陣で画面の下3分の1が
空いたままになった。下から積めば、空くのは両軍のあいだ＝戦場そのものになる。

**地の画は差し替えられる。** `ui/terrainArt.json` に登録すれば
戦闘画面の地がその画になり、無ければ下の線画にそのまま落ちる
（`ui/public/terrain/`）。現在は5地形とも
`scripts/generate-terrain.py` が書き出した画を登録してある
（標高場に陰影を付けた手続き的な地で、絵として描かれたものではない）。**画に兵を描き込まない**ことが要で、
兵は状態から描くもの。焼き込むと兵数が変わっても駒が動かず、
崩れも迂回も表せなくなる。画を敷いたときは上下だけ薄く落として
札と兵を読ませる。

**地形は3層の稜線で描く（画が無いときの既定）。** 遠景・中景・手前に分け、1層ごとに
「陽の当たる面」と「その下に落ちる陰」を対で置く。面を1枚だけ
置いていたときは、色の違う帯が並ぶだけで起伏に見えなかった。
森の梢は丸い房で刻む（直線の帯は生垣の壁に見えた）。
手前の地にも近景を散らす（何も置かないと隊が一様に暗い板の上に立つ）。
地のざらつきは周波数を上げすぎると漆喰の壁のような均一な粒になるので、
粗い粒を薄く重ねるに留める。

**会戦のイメージ画を帯で添える。** 肖像と同じ考え方で、戦場から決定的に
1枚を選ぶ（`ui/battleArt.json` / `ui/public/battles/`）。同じ戦場なら
激突を重ねても画は変わらない。**1枚も置かなければ帯ごと出ない。**
条件を緩める順は「相手と地形」→**「地形」**→「相手」で、地形を相手より
先に残す。この大きさの帯では画の見た目を決めるのが地形のほうで、
砂漠の会戦にガリアの平原の画が出るほうが、そこに写る兵の民が違うことより目につく。
都（イタリア・トラキア）での会戦だけは攻城戦の画に切り替える。

| 兵科の相性 | 騎兵 → 弓 → 歩兵 → 騎兵（有利 1.35 / 不利 0.75） |
|---|---|
| 前進 | 正面からぶつかる。与える損害も受ける損害も等倍 |
| 迂回 | 隣の敵戦列の側面を突く。攻撃 ×1.5、被害 ×1.4 |
| 退却 | 攻撃 ×0.25、被害 ×0.5。士気が戻る |

地形は属州から引く（`data/terrain.json`）。平原は騎兵、丘陵と森林は歩兵、
砂漠は騎兵と弓、渡河点は前へ出た戦列が余計に削られる。

**戦術は戦略の結果を置き換えない。** 戦場で積んだ優劣は
`battlefieldTactics()` が返す**倍率1つ**になり、既存の `giveBattle()` の
攻撃側戦力に掛かるだけで終わる。幅は 0.7〜1.45 と狭く取ってある。
戦術で戦力差をひっくり返せるようにすると、「兵力を養えないから負ける」
という本編の主題が消えるため。

### 指揮官の能力は戦術に効く

指揮官の軍事能力は、戦場では**次の2つにだけ**効かせる。

| 効く先 | 中身 |
|---|---|
| 士気の粘り | 有能なほど隊が崩れにくい（`BATTLE_LEADER_MORALE_SCALE`） |
| 機動の巧拙 | 迂回と退却の成否（`BATTLE_LEADER_MANEUVER_SCALE`） |

**前進には掛けない。** 前進は正面からぶつかるだけで手順が要らず、
そこへ掛けると `giveBattle()` の指揮官補正と同じものを二度掛けることになる。
掛かるのは迂回（側面へ回り込む）と退却（整然と下がる）という、
段取りの要る動きの**上振れ分**だけ。

**基準の交換比は「中庸の将」で取る。** 当初はその指揮官自身で基準を
試算していたが、それだと名将ほど基準の交換比も良くなって差が打ち消し合い、
**軍事1の将のほうが倍率が高く出た**（迂回で 1.065 対 1.041）。
基準を将から切り離したことで、指揮官の技量そのものが倍率に乗るようになった。

計測（各300回、布陣は中庸）:

| 軍司令官の軍事 | 全戦列前進 | 両翼迂回 |
|---|---|---|
| 1 | ×0.93 | ×1.00 |
| 3 | ×0.97 | ×1.03 |
| 5 | ×1.00 | ×1.05 |
| 7 | ×1.03 | ×1.07 |
| 9 | ×1.08 | ×1.13 |
| 10 | ×1.10 | ×1.14 |

**中庸の将が中庸に指すと 1.00 になる。** 調整済みの釣り合いはここが基準。
`giveBattle()` の指揮官補正とは別に上乗せされるので、名将は戦場でも
二重に効くが、幅は 0.93〜1.14 に収まっており、
兵力差をひっくり返すほどではない。

**倍率の基準は「中庸に指したときの交換比」。** 戦場を開いた時点で
中庸の布陣・全戦列前進の一戦を一度だけ試算し、その交換比
（失った兵 ÷ 討ち取った兵）を焼き付けて基準にする。

当初は残存率の差で測っていたが、それだと兵力で劣る側が中庸に指しても
必ず下限 0.7 に張り付いた。少ないほうが多く削られるのは当たり前で、
それは `giveBattle()` が既に戦力差として見ている。戦場でも同じものを
測ると数の差を二重に罰することになり、**劣勢の側が会戦を挑む意味を失う。**

計測（各300回、皇帝が親征、軍事8）:

| 指し手 | 互角 102対100 | 劣勢 85対120 | 優勢 170対90 |
|---|---|---|---|
| 中庸（既定の布陣・全戦列前進） | 0.99 | 1.00 | 1.00 |
| 相性で布陣・全戦列前進 | 0.97 | 1.03 | 1.00 |
| 中庸布陣・両翼迂回 | 1.03 | 1.05 | 1.00 |
| 相性で布陣・両翼迂回 | 1.02 | 1.06 | 1.05 |
| 手薄な側へ全兵科を寄せる | 0.93 | 0.91 | 0.95 |
| 全戦列 退却 | 0.70 | 0.70 | 0.70 |

**中庸がどの兵力比でも 1.0 に収まっているのが要。** 戦闘画面を
挟んでも普通に指すかぎり従来と同じ結果になるので、調整済みの
釣り合いは動かない（同一シードの CSV が1行も変わらないことを確認済み）。
迂回は劣勢のときに最も効き、優勢のときは前進と変わらない。

**ヘッドレス実行は戦場を開かない。** 方針AIは `tick()` を直に呼び、
`tactics` は省略されて 1.0 になる。生存率の計測に戦術は混ざらない。

会戦を含むターンは `beginTurn()` がその年を進めずに戦場を開き、
残りのアクションを戦場に預ける。決着後に `concludeBattle()` が
`tick()` へ渡すので、**コアループの処理順は崩れない。**
セーブも戦闘の途中で取れる。

2つの実際の不具合を計測で見つけて直した。

| 見つけたもの | 直したもの |
|---|---|
| 一度の激突で戦列が丸ごと消え、5回に分ける意味が無かった | `BATTLE_DAMAGE_RATE` 0.5 → 0.16 |
| 正面が食い違うと双方1兵も削れないまま5戦が過ぎた | 正面が空いた戦列は隣の厚い戦列へ回り込む |

### 僭称帝国

動揺しているあいだ、**野心7以上の総督は単に離反するのではなく
ローマ皇帝を僭称して属州ごと帝国から抜ける。** 名は「地域名＋帝国」
（ガリア帝国、ブリタンニア帝国…）。史実のエデッサの戦い（260年、
ウァレリアヌス捕縛）のあとに現れたガリア帝国やパルミラと同じ形。

握られた属州は収入にも保持属州数にも数えない。地図では紫紅で塗り、
帝国の名を重ねる。「僭称帝国を討つ」で取り戻せるが、相手もローマの軍で、
豊かな属州が離れるほど手強くなる（`USURPER_STRENGTH_PER_BASE_TAX`）。

動作の確認（圧倒的な敵との会戦を200回）:

| | 結果 |
|---|---|
| 軍司令官が率いる | 大敗 200 / 200 |
| 皇帝が親征 | 捕虜 64 / 大敗 136 |
| 動揺中に僭称帝国が立つ | 46 / 200 |

会戦も討伐もプレイヤーが選ぶ行動なので、既存の生存率は動かない
（`general` 46/15/13 のまま）。

## 首都と主要属州の占領

都市（`data/cities.json`）は状態を持たない静的なデータだが、
「どの属州に都があるか」だけはこの系統が使う。ローマ＝イタリア、
コンスタンティノポリス＝トラキアというように、rank 10 の都市を含む
属州を**首都属州**、rank 9 以上を含む属州を**主要属州**として引く。

**新しい資源は増やさない。** 作用先は既存の仕組みに限る。

| 起きたこと | 作用する既存の仕組み |
|---|---|
| 自国の都が敵手にある（支配度25未満か僭称帝国の手中） | 属州の動揺。総督の皇帝僭称の確率が跳ね上がる |
| 主要属州（イタリア・アフリカ）を失う | 正統性と元老院支持に上乗せの代償 |
| 東ローマの都（トラキア）を奪う | 東の野戦軍 −35%、残る東方属州の支配度 −20 |
| ペルシアから主要属州を取り返す | ペルシアの戦力 −25%（属州ごと） |

サーサーン朝の都クテシフォンはこの地図では属州ではないので落とせない。
代わりにペルシアが押さえた大都市を取り返すとその戦力を削る形にしてある。

## 東西の分割相続

**統一を果たした皇帝の死に際して、成人した後継者が複数いれば帝国は割れる。**
395年のテオドシウス1世の死がまさにそれで、このゲームの開始点そのもの。
一度統一しても同じことが起きる、という円環をここで閉じる。
後継者が1人なら全土をそのまま引き継ぐ。

**プレイヤーは常に西を操作する。** 西に有利な形で割るが、
**土地の数では差を付けない**（近いトラキア・アシアが西、遠いオリエンス・
エジプトが東方帝の管区）。差は軍と宗主権で付ける。

**統一はその年に局を終わらせない。** かつては `isUnified` を見て勝利として
打ち切っていたので、東方4州すべてを握った状態からの代替わりが永久に起きず、
分割相続が「東方を一部だけ握って死んだ」場合にしか働かなかった。
統一は `unifiedYear` に記録するだけの節目にして、局は476年まで続ける。
勝敗判定は従来どおり476年に行い、統一した局はその記録を添えて表示する。

### 東方帝は独立国ではなく従属国

割れて生まれた東は別の帝国ではない。**西ローマの宗主権のもとに置かれた
東方帝**で、`east.stance` は `war` / `peace` と並ぶ第3の値 `vassal` を取る。

| 西が握るもの | 中身 |
|---|---|
| 兵権 | 東の野戦軍の35%を西の属州防衛の戦力に足せる（`VASSAL_ARMY_SHARE`） |
| 貢納 | 東方属州の収入の45%が毎年の税収に入る（`VASSAL_TRIBUTE_SHARE`） |

分け与えられた側なので軍そのものも兄の帝国の45%しか持てない。
これが「西ローマ優位」の中身で、土地の数では差を付けていない。

**野心の高い東方帝は宗主権を振り払う。** 東方帝は官職と同じ考え方で
**野心だけ**を持つ（2〜9）。野心は独立の確率にのみ効き、
兵権にも貢納にも一切効かない。西の正統性が低いほど通りやすい。
独立されると `vassal` は `peace` に戻り、兵権も貢納も失う。

計測（1年あたりの独立の確率、正統性80）:

| 東方帝の野心 | 独立する確率 |
|---|---|
| 2 | 1.2% |
| 5 | 1.2% |
| 9 | 15.5% |

野心が中庸（5以下）なら基礎確率だけで、実質ずっと従い続ける。
「誰を東に置くかは選べないが、置かれた者次第で帝国の形が変わる」形になっている。

**代替わりの年は侵入が激しくなる。** 蛮族の侵入確率に1.8倍、
ペルシアの攻勢に1.6倍が掛かる。新しい状態は持たせず、
君主の即位年から導いている（`tick()` の純粋性を壊さないため）。

計測では代替わりの動揺を入れても生存率は 46/15/13 → 44/19/12 で、
n=300 の誤差の範囲に収まっている。史実シナリオは `east.provinces` が
空なので貢納も兵権も常に0で、同一シードの CSV は1行も動かない（確認済み）。

## 歴史イベント

`data/events.json` に条件付きテーブルとして持つ。コードに埋め込まない。

主要なもの: 406年ライン渡河、410年ローマ略奪、429年ヴァンダルのアフリカ渡海、
451年カタラウヌムの戦い、455年ヴァンダルのローマ略奪、476年オドアケル。

発火条件は年だけでなく状態にも依存させる。プレイヤーの行動次第で史実が起きない余地を残すこと。

### 発火率の測り方

**到達した局に限って数える。** 分母を全試行にすると、条件が緩いイベントほど
「その年より前に滅んだ局」に薄められて低く出る。判定の窓が開いた年に
生きていた局だけを分母に取る（429年なら窓の開く410年、476年なら460年）。
崩壊した年もその年は生きて迎えているので分母に入れる。

計測（300試行、`general` 方針、初級/中級/上級）:

| イベント | 発火率 |
|---|---|
| 406年 ライン渡河 | 30 / 58 / 81% |
| 410年 ローマ略奪 | 7 / 15 / 20% |
| 429年 アフリカ渡海 | 63 / 63 / 62% |
| 451年 カタラウヌム | 20 / 11 / 10% |
| 455年 ローマ略奪 | 9 / 24 / 25% |
| 476年 オドアケル | 34 / 49 / 38% |

有害なイベントが難易度どおりに開いている。451年だけは有益なので
`historicalSeverityMultiplier` が掛からず、難易度で開かない。

族長の軍事能力を入れる前は 30/63/85・3/8/13・48/57/66・42/33/34・
2/16/24・9/29/35 だった。451年カタラウヌムが 42/33/34 → 33/23/22 に
下がっている。有能な族長の下で攻撃が重くなったぶん、451年まで
健在で迎える局が減ったため。**数字が動いたことは記録に残す。**

**開始戦力は史実に即しているかを問い直したうえで据え置いた。**
395年のフンを90から55に落とす案を測ったが、429年アフリカ渡海が
上級で 66% → 35%、451年カタラウヌムが 34% → 18% まで落ちた。
史実の節目が起きなくなるほうが「史実に即した」から遠いので戻してある。

### 条件が空回りしていないかを見る

条件は書けば効くわけではない。**常に真の条件は無条件の贈り物**であり、
**決して真にならない条件は死に条件**で、どちらも「行動次第で史実が
変わる」という狙いを満たさない。実際に2つとも起きていた。

- **476年** — 「正統性 40 以下 または イタリア 40 以下」を条件にしていたが、
  460年まで生き延びた局の正統性の中央値は 98.8、イタリアの支配度は 100 だった。
  この状態に落ちた帝国は460年より前に崩壊しているので、条件は死んでいた
  （発火率 2/1/0%）。この模型で末期の空洞化が現れるのは `taxBase` のほうで、
  中央値は初級 77 / 中級 56 / 上級 49 と難易度で開く。`taxBase` 42 以下を
  発火口に加えた
- **451年** — 「フンが境外にいない」は事実上いつでも真で、全難易度100%だった。
  軍司令官の在職を条件に足した。史実のカタラウヌムを勝ったのはアエティウスで、
  空位のまま451年を迎えた帝国はこの勝利を得られない

## 勝敗

476年到達時点で判定する。

- **存続** — 帝位が保たれ、`Italia` に加えて1属州以上を保持し、
  かつ `legitimacy` が閾値以上（`SURVIVAL_MIN_LEGITIMACY`）。
  正統性を失ったまま軍と属州だけが残った「名前だけの傀儡国家」は存続ではない
- **崩壊** — 国庫が破綻し野戦軍が消滅、または `Italia` を喪失
- **スコア** — 保持属州数 × `taxBase` × `legitimacy`。到達年も記録する。
  君主能力を変更したセーブは「調整済み」として記録する。
  **難易度も必ず記録する**（異なる難易度のスコアは比較できない）

### 現在のバランス

全機構を入れた状態での生存率（300試行）。数値を触ったらここを測り直す。

| 方針 | 初級 | 中級 | 上級 |
|---|---|---|---|
| `conqueror`（境外へ攻め出る） | 55% | 11% | 5% |
| `general`（将軍を使う） | 53% | 22% | 11% |
| `defensive`（守る） | 47% | 21% | 4% |
| `limited`（傭兵を絞る） | 50% | 14% | 10% |
| `appeaser`（要求を飲む） | 28% | 2% | 0% |
| `passive`（何もしない） | 4% | 1% | 1% |
| `foederati`（傭兵に頼る） | 0% | 0% | 0% |

**中位の方針が潰れていたのを直した。** かつては `defensive` 4% /
`appeaser` 5% / `limited` 3% で `passive` 3% と区別が付かず、
実質 `general` 1択だった。原因は4つで、順に直してある。

| 直したもの | 効いた先 |
|---|---|
| 官職の補充と軍司令官の任命を全方針の既定にした（ハーネス） | 「将軍を使うか」ではなく「宮廷が機能しているか」を測っていた |
| **払える金の要求を飲む**ことも全方針の既定にした（ハーネス） | `general` だけがこれをやっており、他の方針は未応答の要求による攻撃補正と定住されやすさを一方的に背負っていた。`limited` が中級 1% → 13% |
| `DEFEND_GARRISON_GAIN` 10 → 14 | 戦線が同時に2〜3つ開く14勢力では、1属州の守備を厚くする手が野戦軍を増やす手に完全に負けていた（22 まで上げると今度は `defensive` が `general` を追い越す） |
| `DEMAND_PROBABILITY` 0.15 → 0.09 / `DEMAND_LAND_CONTROL_THRESHOLD` 45 → 32 | 要求は1勢力あたりの率なので、勢力を増やすと総量がそのまま増える。土地の要求が特に重いので、出る条件も絞った |
| `TAX_RATE` 0.72 → 0.8 | 14勢力ぶんの戦線を賄えず、最適解以外は破産していた |

**事務と方針を分ける。** 空いた官職を埋め、軍司令官が空位なら任命し、
払える金の要求は飲む。どれもまともな宮廷なら必ずやることで、方針の
違いではない。`general` 方針の持ち味は「いつ将軍を切るか」、
`appeaser` の持ち味は「金だけでなく土地と称号まで飲むか」のほうに
残してある（`passive` は何もしない基準値なのでこれも使わない）。

**`appeaser` が中級2%と最下位に近いのは意図どおり。** 「要求への応答」の
表（金のみ40% / すべて19%）のとおり、土地と称号までまとめて飲む方針は
もともと最下位になるよう作ってある。

**勢力を8から13に増やしたことで全体が難しくなっている。**
8勢力のときは 79/38/15（`general`）だった。合計戦力は 370 → 378 と
ほぼ同じに割り直したが、**被害の総量は戦力の合計ではなく戦闘の回数で
決まる**ため、勢力の数を増やすとそれだけ帝国が削られる。

戻そうとして測った案と、その結果:

| 試した案 | 結果 |
|---|---|
| `ADVANCE_PROBABILITY` を 8/13 倍（採用） | 侵入回数が 2.36→2.31 と元の水準に戻った |
| 定住の恒久コストを属州ごとに一度だけにする（採用） | 定住による二重取りを解消。実際のバグだった |
| 略奪の被害を一律に 8/13 倍 | 40/17/10 → 64/37/13 に戻るが、`passive` 36% が `defensive` 32% を上回り**方針の順序が壊れた** |
| 略奪の被害を勢力の戦力に比例させる | 同じく `passive` 25% > `defensive` 17%。不採用 |
| 定住した属州に他の勢力が割り込めないようにする | 定住は 9.0→4.3 件に戻るが、行き場を失った民が略奪を続け侵入が 2.3→3.5 に増えて悪化 |
| `MIN_STRENGTH_TO_ADVANCE` を 55→72 | 撃退の機会が減って正統性が 64→45 に落ち、さらに悪化 |

**順序の正しさを優先して現在の値を採っている。** 難易度は単調に効き、
`general` が最良、`conqueror` がそれを下回り、`passive` と `foederati` が
底に沈む形は保たれている。ただし中位の3方針が 1〜2% に潰れており、
**13勢力に合わせた本格的な再調整はまだ済んでいない。**

官職と反乱を入れる前は 85/47/19（`general`）だった。郷里と族長の能力を
入れたあとも 79/38/15 で、前回の 79/39/14 から動いていない。郷里は
取らなければ何も起きず、族長の能力は平均すると中庸に近いため。

見たい形は出ている。難易度が単調に効き、`passive` と `foederati` が
底に沈み、軍司令官を扱う方針が最良になる。上級の最良が15%なので、
**上級は「うまくやれば勝てるが多くは負ける」**になっている。
`conqueror` が `general` を全難易度で下回ることも要で、境外へ出る
選択肢を足しても「拡大は延命に劣る」という主題は崩れていない。

統一シナリオ（`unifier` 方針、300試行）は 12 / 4 / 0%。
東ローマの都を落とすと東が折れる仕組みを入れたぶん統一が通りやすくなったので、
東の野戦軍を 175 → 195 に上げて釣り合いを戻した。

**統一率と生存率が別の数字になった。** 統一が局を終わらせなくなったので、
統一した帝国もそのあと476年まで蛮族とペルシアを相手にし続ける。
生存率は 12 / 3 / 1% で、中級では統一した4%のうち一部が統一後に崩れている。
「拡大は延命に劣る」という主題が、拡大した局のその後にも出るようになった。

`TAX_RATE` を上げたぶん西が豊かになり、一度は 49 / 30 / 8% まで
跳ね上がった。中級30%は「拡大はこの帝国には本来無理だった」という
主題から外れているので、**統一シナリオの側だけで**釣り合いを戻した
（東ローマの野戦軍 140 → 175、ペルシアの戦力 150 → 190）。
史実シナリオは `east.provinces` が空で `persia` も眠ったままなので、
この2つを触っても本編の数値は1行も動かない。

## 技術構成

- TypeScript
- UI は React + Tailwind
- 地図は SVG で属州を色分け（外部の地図ライブラリは使わない）
  - 海岸線は Natural Earth 1:50m（属州）/ 1:110m（帝国外の背景陸地）、
    地形（山脈・高原・平原・砂漠・河川）は 1:10m、湖は 1:50m から
    `scripts/generate-map.ts` で静的な SVG パスに変換し `ui/mapPaths.ts` に書き出す。
    実行時に地図ライブラリは読み込まない
  - 属州の領域はローマ期の近似として現代の国境を組み合わせている
  - 表示範囲は西経11°〜東経58°、北緯24°〜59°。シリア・エジプトと
    イラン高原までを収める。52°E で切っていたときはサーサーン朝が
    メソポタミアの帯としてしか映らずラスボスに見えなかった。
    経度の幅を広げたら `WIDTH` も比例して広げる。760 のままだと
    地形だけが viewBox の中で縮み、文字は縮まないので札が重なる
  - 首都と主要都市を点で置く（`data/cities.json`）。都は二重の輪にし、
    大都市には名前を添える。**都市は状態を持たない。** 人物名と同じく
    静的なデータで、触れたときにその属州の支配度・守備隊・税収基礎を
    添えて見せるだけ
  - 西ローマ（支配度で緑〜赤）／東ローマ（帝室の紫）／ペルシア（青緑）／
    帝国外（暗く落とす）の4つに塗り分ける。東ローマとペルシアは属州ではないので
    支配度を持たず一色。バルカン半島は史実では東の管区だが、このゲームは
    西の属州 `Illyricum` にまとめているため紫にはしない
    （地図と属州データを食い違わせない）
  - 軍旗はラバルム（キリストのモノグラム ☧）。コンスタンティヌス以降の
    ローマ軍の標識で、この時代は鷲章ではなくこれを掲げる。
    画像を貼らず線で描き、軍団の竿頭・軍旗の布・親征の金旗・
    君主の欄の地紋で同じ図を使い回す
  - 蛮族は**郷里を面で**描き、属州へ攻め入った軍勢だけを**駒**で描く。
    面と駒で色の意味は揃え、`stance`（敵対・同盟・定住）で変える。
    西が併合した郷里は属州と同じ緑にする。
    定住された属州だけは斜線を重ね、恒久的に失われた税基盤を示す
  - 属州・郷里・東方属州には支配度と**兵力**を併記する。ペルシアは戦力を出す。
    支配度だけでは守りの厚みが読めないため
  - 蛮族・東ローマ・ペルシアの領域に触れると、その君主の肖像・名前・能力と、
    東ローマ／ペルシアの軍司令官が出る（`PowerCard`）。
    「諸国の顔ぶれ」の一覧だけだと、地図の色と誰の土地かが結び付かない
  - 地図上に手で置く地点（属州ラベルの調整、蛮族の待機位置）は経緯度で持ち、
    生成された `projectLonLat` を通す。座標を直に書くと表示範囲を変えたときに
    すべてずれる
  - 起伏の陰影は `feTurbulence` + `feDiffuseLighting` で作る。地形の層は
    毎ターン変化しないためブラウザがラスタ化結果を再利用でき、
    ターン送りの描画負荷は増えない
- 外観は古代ローマ風。羊皮紙と石板の地に帝室紫と金を置き、
  雷紋（メアンダー）で区切る。書体は明朝／セリフに寄せる
  - **外部フォントは読み込まない。** 標準搭載の明朝で碑文のローマ大文字に
    近い骨格が得られるため、配信サイズを増やしてまで足す必要がない
  - 雷紋も画像を持たず `repeating-linear-gradient` で描く
  - 配色と部品は `ui/index.css` の CSS 変数と `.roman-*` クラスに集約する。
    個々の画面に色を直書きしない
- 背景音楽は `ui/romanScore.ts` で Web Audio により合成する。
  **音源ファイルを持たない。** 外部フォントを読み込まず雷紋も CSS で描くのと
  同じ方針で、配信サイズを増やさずに音を付ける（権利の問題も起きない）
  - ドリア旋法・ドローン・アウロス風の音色・ティンパヌムの脈で組む。
    長調短調の機能和声を使わないことが、この時代らしさの要
  - `ui/public/music/theme.mp3` を置くとそちらが優先される。
    **音源はリポジトリに含めない**（権利のあるものを各自で置く）
- セーブは state を JSON 化してダウンロード
- ヘッドレス実行用の CLI を持つ

```
npm run dev     # UI の開発サーバ
npm run build   # UI のビルド
npm run sim     # ヘッドレス実行
npm run typecheck
```

### ディレクトリ

```
src/
  core/          # 純粋関数のみ
    types.ts
    constants.ts # 数値定数はすべてここに集約
    tick.ts
    economy.ts
    military.ts
    diplomacy.ts
    barbarians.ts
    dynasty.ts   # 君主・王朝・継承
    general.ts   # 軍司令官
    officials.ts # プラエトリア長官・属州総督・反乱
    east.ts      # 東ローマ・ペルシア（統一シナリオ）
    battle.ts    # 会戦・僭称帝国
    battlefield.ts # 戦闘画面の戦列マップ（会戦の戦術層）
    partition.ts # 東西の分割相続・従属国
    capitals.ts  # 首都と主要属州の占領
  data/          # provinces.json, factions.json, events.json, east.json,
                 # persia.json, officials.json, terrain.json
  sim/           # ヘッドレス実行（run.ts）
  ui/            # React。ゲームロジックを書かない
    components/BattleMap.tsx    # 戦場の布陣図（駒と矢）
    battleArt.ts / battleArt.json  # 会戦のイメージ画の割り当て
    terrainArt.ts / terrainArt.json # 戦場の地の画（無ければ線画に落ちる）
    public/battles/             # イメージ画（無ければ帯ごと出ない）
    public/terrain/             # 戦場の地の画。兵は描き込まない
```

## 開発ルール

**守ること**

- `core/` に React や DOM を import しない
- `ui/` に計算式を書かない。必ず `core/` の関数を呼ぶ
- `tick()` は副作用を持たない。乱数は `seed` を引数で受け取る
- 数値定数のハードコードを禁止。すべて `core/constants.ts` に置く
  （`sim/` の方針AIの判断閾値はゲームルールではないため例外）
- 属州・勢力・イベントのデータは JSON から読む。コードに直接書かない
- `GameState` の7パラメータを増やさない
- 君主能力を資源として扱わない。あくまで既存式の補正

**やらないこと**

- `core/` を修正するときに `ui/` を同時に触らない。逆も同様
- 頼んでいないリファクタリングをしない
- Phase を飛ばさない。前の Phase のテストが通るまで次に進まない
- バランスが悪いと判断しても、指示なく既存の数値を調整しない

**コミット**

1コミット1トピック。変更内容がわかるメッセージを付ける。

## 実装フェーズ

- **Phase 0** — 型定義（`types.ts`）とデータJSONの雛形のみ。ロジックなし ✅
- **Phase 1** — `tick()` の骨格。収入と支出だけ動く。蛮族もイベントもなし ✅
- **Phase 2** — ヘッドレス実行（`npm run sim -- --turns 81 --out result.csv`）。
  NaN・無限増殖・単調増加を潰す ✅
- **Phase 3** — 蛮族AIと戦闘解決を追加。再びヘッドレスで100回試行し分布を見る ✅
- **Phase 4** — 数値調整。UIは作らない。「主題」の2つのジレンマが数値上で成立するまで繰り返す ✅
- **Phase 4B** — 王朝システム（君主能力・寿命・継承・婚姻外交・能力の変更口）。
  既存の全計算式に補正として入るため、追加後に Phase 4 の検証をやり直す ✅
- **Phase 5** — React UI を被せる。**スマートフォン対応を要件に含める** ✅
- **Phase 6** — 歴史イベントテーブル、セーブ/ロード ✅
- **Phase 7** — 統一シナリオ（東ローマとの戦争、ペルシアの介入）✅
- **Phase 8** — 官職（プラエトリア長官・属州総督）と反乱 ✅

現在のフェーズ: **全フェーズ完了**

残っている課題:

- `passive` 方針は初級3% / 中級0% / 上級0% だが、開始戦力を弱めて測った
  実験では初級7% / 中級11% と初級のほうが低く出た。何もしない帝国は
  撃退による正統性の上乗せを受け取れず、蛮族が弱い初級ほど
  `SURVIVAL_MIN_LEGITIMACY` を割って「名前だけの傀儡国家」で終わるため。
  現在の数値では表面化していないが、正統性の入り口が撃退にしか無いことに
  由来する歪みなので記録しておく
- 郷里の征服は上級では 0%。上級で拡大の余地がないのは主題どおりだが、
  「難しい」ではなく「不可能」に寄っているかは要観察
- `conqueror` が初級で `general` を上回っている（55% 対 53%）。
  n=300 の誤差の範囲だが、「拡大は延命に劣る」を初級でも保つなら
  もう少し離したい。中級・上級では 11/5 対 22/11 と差は明確に出ている
- 蛮族の族長の若年の絵が9枚。壮年・老年は13枚あって勢力ごとに
  一意にできるが、若年だけは12勢力に足りず3つが重なる
- ペルシア王の幼年の肖像は5枚あるが、395〜476年に幼くして即位した
  サーサーン朝の王がいないため出番がない。名簿を史実のままにしてある
